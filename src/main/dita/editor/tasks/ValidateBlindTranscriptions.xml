<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "http://docs.oasis-open.org/dita/v1.1/OS/dtd/task.dtd">
<task id="taskId">
    <title>Validate Blind Transcriptions</title>
    <shortdesc>Validate completed blind transcriptions.</shortdesc>
    <taskbody>
        <context>From the <wintitle>Project Manager</wintitle>:</context>
        <steps>
            <step>
                <cmd>Ensure that the <uicontrol>Blind transcription</uicontrol> check box is
                    de-selected and double-click on the session that you would like to validate. </cmd>
                <stepresult>This will open the <wintitle>Session Editor</wintitle> for the specific
                    session.</stepresult>
            </step>
            <step>
                <cmd>Select the <menucascade>
                        <uicontrol>View</uicontrol>
                        <uicontrol>IPA Validation</uicontrol>
                    </menucascade> menu item.</cmd>
                <stepresult>The <wintitle>IPA Validation</wintitle> view will open, with two tiers:
                        <i>IPA Target Validation</i> and <i>IPA Actual Validation</i>. </stepresult>
            </step>
            <step>
                <cmd>For each group in the tier(s) you are validating, select the most accurate
                    transcription for that group and click <uicontrol>Set</uicontrol>. Alternately,
                    select the most accurate transcription for all groups in a tier and click
                        <uicontrol>Validate &lt;tier></uicontrol>.</cmd>
                <stepresult>The chosen transcriptions will appear in the approprate tier in the
                        <wintitle>Record Data</wintitle> view.</stepresult>
            </step>
            <step>
                <cmd>From the <i>IPA Target </i>and <i>IPA Actual</i> tiers, transcriptions can be
                    further modified as the validator sees fit, by typing or by using the
                        <uicontrol>IPA Map</uicontrol> (<userinput>CMD/CTRL+M</userinput>).</cmd>
            </step>
            <step>
                <cmd>Repeat Steps 3 and 4 until validation has been completed for the entire
                    session.</cmd>
            </step>
        </steps>
    </taskbody>
</task>
