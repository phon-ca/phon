<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "http://docs.oasis-open.org/dita/v1.1/OS/dtd/task.dtd">
<task id="taskId">
    <title>Split Group</title>
    <shortdesc>Split the contents of a group.</shortdesc>
    <taskbody>
        <context>From the <wintitle>Record Data</wintitle> view:</context>
        <steps>
            <step>
                <cmd>Position the cursor inside a group in the <i>Orthography</i> tier where the
                    group should be split (at or after a space between words).</cmd>
            </step>
            <step>
                <cmd>Click the <uicontrol>Split group</uicontrol> button in the <wintitle>Record
                        Data</wintitle> view, or select the <menucascade>
                        <uicontrol>View</uicontrol>
                        <uicontrol>Record Data</uicontrol>
                        <uicontrol>Split group</uicontrol>
                    </menucascade> (<userinput>OPT/ALT+K</userinput>) menu item.</cmd>
                <stepresult>The group data will be split into two separate groups. If only one word
                    was present in the group, an empty preceding group will be created. Only data in
                    the <i>Orthography</i> tier will be divided. Other grouped tiers may require
                    manual editing. </stepresult>
            </step>
        </steps>
    </taskbody>
</task>
