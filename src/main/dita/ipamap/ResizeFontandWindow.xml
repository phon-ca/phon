<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE task PUBLIC "-//OASIS//DTD DITA Task//EN" "http://docs.oasis-open.org/dita/v1.1/OS/dtd/task.dtd">
<task id="taskId">
    <title>Resize Font and Window</title>
    <shortdesc>Change the font display size and window size of the <wintitle>IPA
        Map</wintitle>.</shortdesc>
    <taskbody>
        <context>From the <wintitle>IPA Map</wintitle> window:</context>
        <steps>
            <step>
                <cmd>Right-click (<userinput>CTRL+click</userinput>) within any section in the
                        <wintitle>IPA Map</wintitle> and move the <uicontrol>Font size</uicontrol>
                    slider to the preferred font size.</cmd>
                <stepresult>The characters in the <wintitle>IPA Map</wintitle> will be displayed in
                    the selected font size; the window will resize itself accordingly.<note>The font
                        display size in the <wintitle>IPA Map</wintitle> will not affect the size of
                        characters when input into text fields.</note></stepresult>
            </step>
        </steps>
    </taskbody>
</task>