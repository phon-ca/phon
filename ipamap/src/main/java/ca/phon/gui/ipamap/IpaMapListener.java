package ca.phon.gui.ipamap;

/**
 * Listen for button events in the
 * ipa map.
 */
public interface IpaMapListener {
	
	/**
	 * Called when an ipa button is pressed.
	 * 
	 * @param the text of the ipa button
	 */
	public void ipaMapEvent(String txt);

}
